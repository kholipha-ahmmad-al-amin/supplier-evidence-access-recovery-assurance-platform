import{mkdirSync,readFileSync,renameSync,writeFileSync}from'node:fs';import{dirname}from'node:path';export class JsonRecoveryStore{constructor(file){this.file=file}load(){try{const x=JSON.parse(readFileSync(this.file,'utf8'));return{recoveries:x.recoveries??[],events:x.events??[]}}catch(error){if(error.code==='ENOENT')return{recoveries:[],events:[]};throw error}}save(state){mkdirSync(dirname(this.file),{recursive:true});const temp=`${this.file}.tmp`;writeFileSync(temp,JSON.stringify(state,null,2));renameSync(temp,this.file)}}
