import express from'express';import{join}from'node:path';import{JsonRecoveryStore}from'./store.mjs';import{RecoveryService}from'./domain.mjs';const store=new JsonRecoveryStore(process.env.DATA_FILE||join(process.cwd(),'data','recoveries.json')),service=new RecoveryService(store.load(),state=>store.save(state)),app=express(),safe=fn=>(req,res)=>{try{fn(req,res)}catch(error){res.status(400).json({error:error.message})}};app.use(express.json());app.get('/health',(_,res)=>res.json({status:'ok',service:'supplier-evidence-access-recovery-assurance-platform'}));app.get('/recoveries',(_,res)=>res.json(service.state.recoveries));app.post('/recoveries',safe((req,res)=>res.status(201).json(service.open(req.body))));app.post('/recoveries/:id/validate-restore',safe((req,res)=>res.json(service.validateRestore(req.params.id,req.body.actor,req.body.note))));app.post('/recoveries/:id/approve',safe((req,res)=>res.json(service.approve(req.params.id,req.body.actor))));app.post('/recoveries/:id/reconcile',safe((req,res)=>res.json(service.reconcile(req.params.id,req.body.actor))));app.post('/recoveries/:id/assure',safe((req,res)=>res.json(service.assure(req.params.id,req.body.actor))));app.get('/recoveries/:id/history',safe((req,res)=>res.json(service.history(req.params.id))));app.listen(process.env.PORT||60700,'0.0.0.0');
